# Local AWS CLI profile name. Never put credentials in this file.
AWS_PROFILE=your-aws-profile

# Optional Bedrock inference profile override. The app defaults to Nova Micro in ap-south-1.
BEDROCK_MODEL_ID=your-bedrock-inference-profile
